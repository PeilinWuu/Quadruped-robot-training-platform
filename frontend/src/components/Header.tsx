import { Bell, Box, BrainCircuit, Database, Flame, LogOut, Settings, UserRound } from 'lucide-react'
import type { AuthUser } from '../services/authService'

const nav = [{ label: '场景管理', icon: Box }, { label: '训练管理', icon: BrainCircuit }, { label: '算法管理', icon: Database }, { label: '仿真监控', icon: Flame, active: true }, { label: '数据管理', icon: Database }, { label: '系统设置', icon: Settings }]
export function Header({ user, onLogout }: { user: AuthUser; onLogout: () => void }) { return <header className="topbar"><div className="brand"><span className="brand-mark"><Flame size={19} /></span><div><strong>建筑火灾 · 四足机器人训练平台</strong><small>FIRE-RESCUE AUTONOMOUS SYSTEM</small></div></div><nav>{nav.map(({ label, icon: Icon, active }) => <button key={label} className={active ? 'active' : ''} onClick={() => window.dispatchEvent(new CustomEvent('reserved-action', { detail: label }))}><Icon size={16} />{label}</button>)}</nav><div className="user"><Bell size={17} /><span className="avatar"><UserRound size={16} /></span><span className="user-name" title={user.email}>{user.displayName}</span><button className="logout-btn" aria-label="退出登录" title="退出登录" onClick={onLogout}><LogOut size={15}/></button></div></header> }
